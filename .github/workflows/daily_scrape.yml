name: Daily Scrape

on:
  schedule:
    - cron: '30 4 * * *'  # Runs daily at 10:00 AM IST (UTC +5:30)
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🚀 Trigger Render backend to fetch & cache
        run: |
          curl -X POST https://archibaldnews-backend.onrender.com/news/fetch-and-cache